import WorkWithMe from "../component/work";

export const metadata = {
  title: "Tim — Work",
  description: "Working with Timothy Isah — systems engineering, fintech, and building things that last.",
};

const Work = async () => {

  return (
    <div>
      <WorkWithMe />

    </div>
  );
};

export default Work;
